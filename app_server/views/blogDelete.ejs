<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<link rel = 'stylesheet' href = '/stylesheets/style.css' />
		<link rel = 'stylesheet' href = '/css/bootstrap.min.css' />
	</head>
	<body>
		<% include ./partials/nav %>
		<div class="card border-primary" style="width: 60%">
			<form method="POST" name="userForm" action="/blogDelete/<%= blog._id %>"  style="padding: 15px">
				<div class="form-group">
					<label for="summary" class="text-primary">Blog Title</label>
					<input readonly class="form-control" id="blogTitle" type="text" name="blogTitle" value="<%= blog.blogTitle %>">
					<label for="detail" class="text-primary">Blog Text</label>
					<textarea readonly class="form-control" id="blogText" name="blogText" rows="3"><%= blog.blogText %></textarea>
				</div>
				<input type="submit" class="btn btn-danger float-right" value="Delete Blog">
				<a class="btn btn-primary float-right" style="margin-right: 25px" href = "/blogList">Cancel</button>
			</form>
		</div>
	</body>
</html>
